'use strict';

/* Directives */


angular.module('chetongxiang.directives', []).
  directive('appVersion', ['version', function(version) {
    return function(scope, elm, attrs) {
      //elm.text(version);
      $scope.navactive = "v1.2";
      console.log($scope.navactive);
    };
  }]);
  
  
//angular.module('chetongxiang.directives', []).
//directive('NavActive', ['activeName', function(activeName) {
//  return function(scope, elm, attrs) {
//    $scope.navactive = activeName;
//    console.log($scope.navactive);
//  };
//}]);
//
